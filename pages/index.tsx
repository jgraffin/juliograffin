import type { NextPage } from "next";

import { Main, Container } from "../styles/global";

const Home: NextPage = () => {
  return (
    <Main>
      <Container>container</Container>
    </Main>
  );
};

export default Home;
